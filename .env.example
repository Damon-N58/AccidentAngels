# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Auth
NEXTAUTH_SECRET=

# Paystack
PAYSTACK_SECRET_KEY=
PAYSTACK_WEBHOOK_SECRET=

# Cron (protects /api/cron/billing and /api/cron/check-expiry)
CRON_SECRET=

# Authenticates incoming webhook calls from the Nineteen58 platform to
# app/api/webhooks/whatsapp/parent-onboarding/route.ts
N58_WEBHOOK_SECRET=

# Africa's Talking SMS (OTP). If unset, OTPs are logged to console instead of sent.
AT_API_KEY=
AT_USERNAME=
AT_SENDER_ID=AccidentAngels

# Nineteen58 WhatsApp (used by lib/whatsapp/nineteen58.ts for OTP delivery).
# If unset, messages are logged to console instead of sent.
NINETEEN58_API_URL=
NINETEEN58_API_KEY=

# App URLs (defaults point to production; override for local/staging)
NEXT_PUBLIC_DRIVER_URL=https://driver.accidentangels.co.za
NEXT_PUBLIC_PARENT_URL=https://parent.accidentangels.co.za
NEXT_PUBLIC_ADMIN_URL=https://admin.accidentangels.co.za

# Which sub-app localhost/unknown hosts proxy to: driver | parent | admin
NEXT_PUBLIC_LOCAL_APP=driver

# Set to 'true' to bypass real OTP delivery and use a fixed demo code
DEMO_MODE=false